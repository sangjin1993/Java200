package chapter.s033;
// (위도, 경도)를 저장하는 클래스
public class Geo {
	public double latitude;
	public double longitude;
}

/*
 * Geo 클래스 선언
 * 위도와 경도를 저장할 수 있는 double 타입 latitude, longitude를 선언한다. 기본 타입 멤버는 0에
 * 관련된 값으로 자동 초기화된다. 여기서 latitude와 longitude는 모두 0.0으로 초기화 된다.
 */
